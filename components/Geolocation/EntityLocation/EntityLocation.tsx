import styles from './EntityLocation.module.scss'

const EntityLocation = (props: any) => {
    return (
        <div className={styles.EntityLocation}>
        </div>
    )
}

export default EntityLocation