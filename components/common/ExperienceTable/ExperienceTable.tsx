import styles from './ExperienceTable.module.scss'

const ExperienceTable = (props: any) => {
    return (
        <div className={styles.ExperienceTable}>
        </div>
    )
}

export default ExperienceTable