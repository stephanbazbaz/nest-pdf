import styles from './ContactsTable.module.scss'

const ContactsTable = (props: any) => {
    return (
        <div className={styles.ContactsTable}>
        </div>
    )
}

export default ContactsTable